import { Typography } from 'radiance-ui';
import styled from '@emotion/styled';

export const NoToDos = styled(Typography.Heading)`
  text-align: center;
`;

export const Filters = styled.div`
  display: flex;
`;